@model List<MemorialRoyaleProject.Models.WorkExample>
@{
    ViewData["Title"] = "Примеры выполненных работ";
}

<h3 class="text-center"> Примеры выполненных работ</h3>
<div class="text-center">
    @if(User.IsInRole("Admin"))
    {
        <a class="btn btn-success btn-sm mb-3" asp-action="Create">Add new</a>
    }
</div>
<div class="example-card text-center">
    @foreach (var workExample in Model)
    {
        var photoPath = "~/images/WorkExamples/" + (workExample.ImagePath ?? "no_image.jpg");
        <img src="@photoPath" asp-append-version="true" />
        @if(User.IsInRole("Admin"))
        {
            <a class="btn btn-primary btn-sm" style="opacity: .8 !important;" asp-action="Edit" asp-route-id="@workExample.Id">Edit</a>
            <a class="btn btn-danger btn-sm" style="opacity: .8 !important;" asp-action="Delete" asp-route-id="@workExample.Id">Delete</a>
        }
    }
</div>
<div class="text-center" style="margin-bottom: 70px;">
    <p>Компания «Memorial Royal» ставит своей задачей оказание качественных и оперативных услуг. Мы предлагаем широкий ассортимент памятников и сопутствующей продукции. Всегда готовы предоставить консультацию, касающуюся характеристик натурального камня.
Если Вас заинтересовал памятник из гранита, купить его в нашей компании можно по доступным ценам. Большой выбор моделей позволит подобрать вариант лучше всего передающий Ваши чувства и отношение к ушедшему. Позвоните, чтобы узнать больше.</p>
</div>